<h1>Edit</h1>

<%= button_to "Destroy this planet", @planet, method: :delete if policy(@planet).destroy? %>


<%= simple_form_for(@planet) do |f| %>
  <%= f.input :name %>
  <%= f.input :location %>
  <%= f.input :price_per_night %>
  <%= f.input :planet_photo %>
  <%= f.submit %>
<% end %>
